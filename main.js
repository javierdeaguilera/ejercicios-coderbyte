function add(n1, n2) {
    console.log(n1 + n2);
}
add(3, 2);



/*
Haga que la función LetterChanges(str) tome el parámetro str que se está pasando y lo modifique utilizando el siguiente algoritmo. Reemplace cada letra de la cadena con la letra que le sigue en el alfabeto (es decir, c se convierte en d, z se convierte en a). Luego escribe en mayúsculas cada vocal en esta nueva cadena(a, e, i, o, u) y finalmente devuelve esta cadena modificada.
*/

// Examples

// Input: "hello*3"
// Output: Ifmmp*3

// Input: "fun times!"
// Output: gvO Ujnft!

const str = 'hello*3'

function LetterChanges(str) {
    
    return str
}

LetterChanges(str)

//=========================================

// function uppCaseMe() {
//     return arguments[0].toUpperCase();
// }

// myString = "a string like no other"

// myNewString = myString.replace(/[aeiou]/g, uppCaseMe);

// console.log(myString);
// console.log(myNewString)

//==========================================
